- name: frontendsetup
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest

    - name: start Nginx
      ansible.builtin.systemd_service:
        state: restarted
        name: nginx

