- name: frontendsetup
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest



                    # (Optional) Set to yes if src refers to a remote file

    - name: start Nginx
      ansible.builtin.systemd_service:
        state: restarted
        name: nginx

